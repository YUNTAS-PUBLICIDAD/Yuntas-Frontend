---
import "swiper/css";
import "swiper/css/effect-fade";
import "swiper/css/pagination";
import "./HeroSlider.css";
import imagen1 from "../../assets/images/inicio/slider/diseño-luces-led-en-tunel.webp";
import imagen2 from "../../assets/images/inicio/slider/estructura-iluminada-con-luces-led-para-exteriores.webp";
import imagen3 from "../../assets/images/inicio/slider/slider_3.webp";
import slider from "../../assets/images/inicio/slider/luces-1.webp";

import { Image } from "astro:assets";

// Configuración estructurada de slides con metadatos completos
const slidersData = [
  {
    src: slider,
    title: "Túnel iluminado con luces LED",
    alt: "Túnel iluminado con luces LED, diseño moderno de iluminación en espacios urbanos, tecnología visual para entornos impactantes y personalizados.",
  },
  {
    src: imagen2,
    title: "Instalación arquitectónica curva",
    alt: "Instalación arquitectónica curva, iluminación LED roja, estructura exterior futurista, ideal para eventos nocturnos y espacios públicos envolventes.",
  },
  {
    src: imagen3,
    title: "Instalaciones de paneles electrónicos",
    alt: "Instalaciones de paneles electrónicos y sistemas visuales para negocios",
  },
];
---

<section class="relative w-full h-screen select-none overflow-hidden bg-black">
  <div class="hero-swiper">
    <div class="swiper">
      <div class="swiper-wrapper">
        {
          slidersData.map((slide, index) => (
            <div class="swiper-slide">
              <Image
                src={slide.src}
                alt={slide.alt}
                title={slide.title}
                widths={[320, 540, 600, slide.src.width]}
                sizes="(max-width: 420px) 320px, (max-width: 720px) 540px, (max-width: 1024px) 600px, 100vw"
                quality={70}
                loading={index === 0 ? "eager" : "lazy"}
                class="w-full h-full object-cover absolute inset-0"
              />
              {/* Texto sobre el slider */}
              <div class="absolute inset-0 flex flex-col justify-center sm:justify-end p-4 sm:p-20 pointer-events-none z-20 space-y-2">
                <h1 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl xl:text-6xl Montserrat font-semibold text-white sombra-title">
                  ESPECIALISTAS EN
                </h1>
                <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl Montserrat font-black text-white sombra-title">
                  DISEÑAR TU ESPACIO
                </h1>
                <div class="flex flex-col sm:flex-row justify-between items-center space-x-4">
                  <h2 class="text-lg sm:text-xl md:text-xl lg:text-2xl xl:text-3xl text-white font-semibold">¡Somos <span class="font-bold uppercase">Yuntas</span> Publicidad!</h2>
                  <a href="" class="uppercase font-black text-white py-2 px-8 bg-[#23C1DE] rounded-2xl">Contáctanos</a>
                </div>
              </div>
            </div>
          ))
        }
      </div>

      <!-- Controles -->
      <div class="swiper-pagination"></div>
    </div>
  </div>
</section>

<style >
  .hero-swiper {
    width: 100%;
    height: 100%;
  }

  .swiper {
    width: 100%;
    height: 100%;
  }
</style>

<script>
  import Swiper from "swiper";
  import { Autoplay, EffectFade, Pagination } from "swiper/modules";

  document.addEventListener("DOMContentLoaded", () => {
    new Swiper(".swiper", {
      modules: [Autoplay, Pagination, EffectFade],
      loop: true,
      effect: "fade",
      autoplay: {
        delay: 5000,
        disableOnInteraction: false,
      },
      pagination: {
        el: ".swiper-pagination",
        clickable: true,
      },
    });
  });
</script>
